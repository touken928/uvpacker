[project]
name = "cli-demo"
version = "0.1.0"
description = "Minimal console CLI example for uvpacker."
requires-python = "==3.14.*"
dependencies = []

[project.scripts]
cli-demo = "cli_demo.main:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
